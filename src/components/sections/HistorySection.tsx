import { SectionPlaceholder } from "@/components/ui/SectionPlaceholder";

export function HistorySection() {
  return (
    <SectionPlaceholder
      id="history"
      eyebrow="History"
      title="From the Viking Age to Modern Norway"
      description="Important events, independence, conflicts, achievements and influential historical figures."
    />
  );
}
